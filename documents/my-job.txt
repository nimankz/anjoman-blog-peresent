SHOW
  GET /resources/:id
LIST
  GET /resources
CREATE
  POST /resources
    (data in body)
UPDATE
  PUT /resources/:id
    (update data in body)
DELETE
  DELETE /resources/:id



--------------------

Signup
  POST /users (data in body: email, pass, name)
login
  POST /sessions (data in body: user, pass)

Suppose that the first

get list of announcements for every user.
  GET /announcements
create a new announcement as an admin (publish)
  POST /announcements (all data needed in body)
show a single announcement as an admin
  GET /announcements/:announcement_id
delete a single announcement as an admin user
  DELETE /announcements/:announcement_id
update a single announcement as an admin user
  PUT /announcements/:announcement_id (all data needed in body)


get list of events for every user.
  GET /events
create a new event as an admin (publish)
  POST /events (all data needed in body)
show a single event as an admin
  GET /events/:event_id
delete a single event as an admin user
  DELETE /events/:event_id
update a single event as an admin user
  PUT /events/:event_id (all data needed in body)


create a new article by user
  POST /articles
see the list of all unprocessed articles by admin
  GET /articles?staus=published  (status can be: publish, rejected, accepted)
accept or reject articles by admin
  PUT /articles/:article_id (data of the update) (only admins can do this)


See all users by admin
  GET /users

comment on a post (can be event, announcements)
  POST /comments (postId and postType darj kon dar body)






Auth
Blog




------
make announcement, article, event, comment controllers.
  implement all the handlers. use user-controller and session-controller as an example
read decorateWithAuth and decorateWith and understand how they work


